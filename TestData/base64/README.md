# base64 test files #

"shrinkit.bsc" and "zlink-nn.bsq" were originally posted to the
comp.binaries.apple2 Usenet newsgroup by their authors.

Because these are text files, git will change the line endings to match
the current system.
